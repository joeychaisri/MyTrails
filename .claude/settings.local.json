{
  "permissions": {
    "allow": [
      "Bash(node --version)",
      "Bash(npm --version)",
      "Bash(cmd.exe /c \"node --version && npm --version\")",
      "Bash(cmd.exe /c \"where node && node --version\")",
      "Bash(cmd.exe /c \"where bun && bun --version\")"
    ]
  }
}
