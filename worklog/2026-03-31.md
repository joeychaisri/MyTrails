# Worklog — 2026-03-31

## 1. Race Operations — Runner Demographics: รวม Age Range + Gender

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`

### สิ่งที่เปลี่ยน
- ยุบ Age Range section กับ Gender section ให้เหลือ section เดียว
- แต่ละ age range bar แสดงเป็น split bar สีน้ำเงิน (Male) + ชมพู (Female)
- ตัวเลขด้านขวาเปลี่ยนเป็น `XM / YF`
- Legend รวมอยู่ที่ header ของ card
- เพิ่มช่วงอายุ **50–59** และ **60+** แทน `50+` เดิม รวมเป็น 5 ช่วง:
  - Under 30 / 30–39 / 40–49 / **50–59** / **60+**
- ปรับ `ageRanges` calculation ให้แต่ละ entry มี `{ label, male, female, count }`

---

## 2. Race Operations — Registration Fill Rate: แยกเพศต่อ Category

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`

### สิ่งที่เปลี่ยน
- เพิ่ม legend Male/Female ที่ header ของ card
- Progress bar แต่ละ category เปลี่ยนจาก single color เป็น split bar น้ำเงิน+ชมพู
- ตัวเลข `XM / YF` ใต้แต่ละ bar
- คำนวณ gender breakdown โดย match `p.distance` กับ prefix ชื่อ category (`cat.name.split(" ")[0]`)

---

## 3. Race Operations — Shirt Size Summary: เพิ่ม Finisher Shirt

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### Mock Data (`mockData.ts`)
- เพิ่ม `finisherShirtSizeBreakdown` object (XS–XXL, ตัวเลขประมาณ 70% ของ event shirt)
- เพิ่ม export ให้ใช้งานได้จากภายนอก

### UI (`EventManagerHub.tsx`)
- Import `finisherShirtSizeBreakdown` เพิ่ม
- `shirtData` เปลี่ยนจาก `{ size, count }` เป็น `{ size, event, finisher }`
- `shirtEntries` ใน case overview2 คำนวณรวม finisher ด้วย
- **Shirt Size Summary card:** แต่ละ size แสดง 2 แถบซ้อน (primary = Event, เขียว = Finisher) + ตัวเลขสีต่างกัน
- **T-Shirt Breakdown chart** (Merchandise Summary): เปลี่ยนเป็น grouped bar 2 series พร้อม `Legend`
- Import `Legend` จาก recharts

---

## 4. Race Operations — Top Nationalities Modal: Redesign

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`

### สิ่งที่เปลี่ยน
- เพิ่ม state `natModalSearch` สำหรับ filter ภายใน modal
- Import icons `Globe`, `ChevronRight` เพิ่ม
- **Trigger button:** จาก plain text → ghost Button พร้อม count badge + ChevronRight icon

**Modal redesign:**
| ส่วน | รายละเอียด |
|---|---|
| Header | Globe icon (bg primary/10) + title "Runner Nationalities" + subtitle "X countries · Y runners" |
| Search | Input พร้อม Search icon, bg muted, clear เมื่อปิด modal |
| Rank badges | อันดับ 1–3 มี badge สี gold/silver/bronze, ที่เหลือเป็นตัวเลขธรรมดา |
| List rows | flag + country name + progress bar เต็มแถว + count + percentage |
| Empty state | Globe icon + "No countries found" เมื่อ search ไม่เจอ |
| Interaction | hover highlight + divider เส้นบางระหว่าง row |

- `nationalitySorted` (ทุก country) แยกจาก `nationalityTop5` เพื่อใช้ใน modal

---

## 5. BIB Assignment — หน้าใหม่ + Import Wizard

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### Mock Data (`mockData.ts`)
- Clear `bibNo: ""` ให้กับ 25K runners ทั้ง 20 คน (เดิมมีเลข) เพื่อ simulate สถานะ pre-assignment
- รวมกับ p8 (Krit), p97 (Akira) ที่ว่างอยู่แล้ว → **TBA รวม 22 คน**

### Type & Navigation (`EventManagerHub.tsx`)
- เพิ่ม `"bib"` ใน `HubSection` type
- เพิ่ม sidebar item `{ id: "bib", label: "BIB Assignment", icon: Hash }`
- Import `useState, useRef` (เพิ่ม useRef)
- Import icons `Upload`, `FileSpreadsheet` เพิ่ม

### State เพิ่มใหม่ (12 ตัว)
```ts
bibSearch, bibFilter, bibCategoryFilter          // filter/search ใน table
importOpen, importStep                            // dialog control
importFileReady, importFileName                   // file upload state
importColBib, importColId                         // column mapping step 2
importConflictChoice                              // radio choices ใน step 3
editingBibId, editingBibValue                     // inline edit ใน table
fileInputRef                                      // useRef สำหรับ hidden file input
```

### หน้า BIB Assignment (`case "bib"`)

**Stat Cards (4 ใบ)**
- Total Runners / Assigned (สีเขียว + %) / TBA (สีเหลือง) / Conflicts (สีแดง)

**Progress Bar**
- แสดง assignment progress คิดจาก bibNo !== "" / participants.length

**Action Bar**
- `[Import Excel]` เปิด wizard, `[Download Template]` placeholder
- Filters: Category dropdown + Status dropdown (All/Assigned/TBA) + Search input

**Editable Table**
- Columns: # / BIB No. / Name / Category / Gender / Status
- Click ที่ BIB cell → เปลี่ยนเป็น `<Input>` inline
  - Enter = save, Escape = cancel, blur = save
- Status badge: สีเขียว Assigned / สีเหลือง TBA
- แสดงสูงสุด 50 rows, มี footer บอกจำนวนที่เหลือ

### Import Wizard Dialog (4 Steps)

**Step indicator** — stepper bar ที่ header dialog พร้อม filled/active/inactive states

| Step | หน้าตา |
|---|---|
| 1 Upload | Drag & drop zone + `<input type="file" accept=".xlsx,.csv">` จริง + Download Template button |
| 2 Map Columns | Preview 5 rows จาก "Excel" + highlight column ตามที่เลือก + dropdowns map BIB column / Identifier column |
| 3 Preview & Resolve | Summary chips (18 matched / 1 conflict / 3 unmatched) + Tabs 3 แท็บ |
| 4 Done | Success screen + ตัวเลข assigned/skipped |

**Step 3 Tabs detail:**
- **Matched**: list rows แสดง BIB + name + category + ✓ icon
- **Conflicts**: card warning สีเหลือง พร้อม radio buttons เลือก runner ที่จะ keep BIB
- **Unmatched**: list rows แสดง BIB + name + identifier + row number (skipped)

**Apply logic (`handleImportApply`)**
- Map email → BIB จาก mockMatched
- เพิ่ม conflict winner จาก `importConflictChoice` state
- `setParticipants` update bibNo จริงใน component state
- Move to Step 4

**Mock import data (hardcoded สำหรับ prototype)**
- 18 matched rows (25K runners)
- 1 conflict: BIB 203 assigned ให้ 2 คน
- 3 unmatched rows (email ไม่ตรงกับ system)

---

## 6. Promotions Tab — Full CRUD + Bulk Generate

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### Mock Data (`mockData.ts`)
- เพิ่ม interface `DiscountCodeUsage` (`name`, `email`, `usedAt`, `orderId`)
- เพิ่ม fields ใน `DiscountCode`: `isActive: boolean`, `categories: string[]`, `usages: DiscountCodeUsage[]`
- อัปเดต `mockDiscountCodes` ทั้ง 3 records ให้มี fields ใหม่ครบ พร้อม mock usage history

### Type & Navigation (`EventManagerHub.tsx`)
- เพิ่ม `"promotions"` ใน `HubSection` type
- เพิ่ม sidebar item `{ id: "promotions", label: "Promotions", icon: Tag }`
- Import icons `Trash2`, `Copy` เพิ่ม

### State เพิ่มใหม่ (10 ตัว)
```ts
discountCodes[]          // local state จาก mockDiscountCodes
promoSearch              // search bar
createPromoOpen          // dialog create
editPromoOpen            // dialog edit
editingPromo             // promo ที่กำลัง edit
usageDetailOpen          // dialog usage detail
usageDetailCode          // promo ที่ดู usage
bulkGenOpen              // dialog bulk generate
deletePromoId            // confirmation delete
promoForm, bulkGenForm   // form state
```

### หน้า Promotions (`case "promotions"`)

**Summary Stats (3 ใบ)**
- Active Codes / Total Used / Average Discount

**Action Bar**
- Search input + `[+ Create Code]` + `[Bulk Generate]`

**Discount Code Table**
- Columns: Code / Type / Discount / Limit / Used / Valid Until / Categories / Status / Actions
- Status: toggle switch Active/Inactive — `handleToggleActive()` update state จริง
- Copy button — copy code ไป clipboard
- 3-dot menu: Edit / View Usage / Delete

**Create / Edit Dialog**
- Fields: Code / Discount value / Type (% or Fixed) / Usage Limit / Valid Until / Categories (toggle chips) / Active toggle
- Validate required fields ก่อน save

**Usage Detail Dialog**
- แสดง table: Name / Email / Used At / Order ID
- Header บอก code + จำนวน used/limit

**Bulk Generate Dialog**
- Fields: Prefix / Count (จำนวนโค้ด) / Discount / Type / Usage Limit / Valid Until / Categories
- Generate โค้ดรูปแบบ `{PREFIX}-XXXX` แบบสุ่ม 4 หลัก
- Push เข้า `discountCodes` state จริง

**Delete Confirmation Dialog**
- แสดงชื่อ code + warning "cannot be undone"

---

## 7. Race Ops (New ✦) — UI Redesign Preview Tab

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`

### เหตุผล
สร้าง tab ใหม่ควบคู่กับ Race Operations เดิม เพื่อให้เปรียบเทียบ UI ได้โดยไม่กระทบของเก่า

### Type & Navigation
- เพิ่ม `"overview3"` ใน `HubSection` type
- เพิ่ม sidebar item `{ id: "overview3", label: "Race Ops (New ✦)", icon: Sparkles }`
- Import เพิ่ม: `CalendarDays`, `TrendingUp`, `Sparkles` จาก lucide-react
- Import เพิ่ม: `AreaChart`, `Area` จาก recharts

### Color Palette ใหม่
| ชื่อ | สีเดิม | สีใหม่ | เหตุผล |
|---|---|---|---|
| Male | `#3B82F6` (Tailwind blue-500) | `#4A7FC1` | อุ่นขึ้น ชนกับ orange brand น้อยลง |
| Female | `#EC4899` (Tailwind pink-500) | `#C4607A` | Dusty rose แทน hot pink — subtle กว่า |
| Finisher shirt | `#10B981` (Tailwind emerald) | `#2D9166` | Forest green เข้มขึ้น มีน้ำหนักกว่า |
| Nationality bars | primary ทุก bar | primary + opacity gradient | rank 1 = 100%, rank 5 = 40% |

### Stats Cards — เปลี่ยนจาก 3 → 4 cards
| Card | ของใหม่ |
|---|---|
| Total Revenue | + icon DollarSign + accent color |
| Tickets Sold | รวม `sold / capacity` + inline progress bar + "X remaining" |
| Fill Rate | Donut chart 60px + % ตรงกลาง + "X slots left" |
| Race Day | Countdown "X days" + วันที่ format `dd MMM yyyy` |

### Chart Upgrades
| Chart | เดิม | ใหม่ |
|---|---|---|
| Revenue | LineChart | AreaChart + gradient fill ใต้เส้น |
| Demographics | Stacked bars ทางเดียวกัน | Butterfly chart (M←→F จาก center) |
| Shirt Sizes | 2 progress bars บางๆ ต่อ size | Grouped BarChart แนวตั้ง (Recharts) |

### Registration Fill Rate — เพิ่ม
- Label "Almost Full" / "Full" badge สี primary เมื่อ ≥ 80% / ≥ 99%
- แสดง "X remaining" ใต้ทุก bar

### Top Nationalities — เปลี่ยน Flag
- จาก emoji flags (ไม่ render ใน Chrome/Linux) → `<img>` จาก `flagcdn.com/24x18/{code}.png`
- `onError` handler ซ่อน img ถ้าโหลดไม่ได้

### Recent Activity — เพิ่ม Left Border Accent
- `border-l-[3px]` สีต่างกันตาม type:
  - registration → `#2D9166` (green)
  - payment → primary (orange)
  - cancellation → destructive (red)
  - refund → warning (yellow)
