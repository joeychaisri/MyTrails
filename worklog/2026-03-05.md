# Worklog — 2026-03-05

## 1. EventManagerHub — Race Operations (Overview 2)

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### สิ่งที่เพิ่มใหม่
- สร้างหน้าใหม่ **Race Operations** (sidebar item `overview2`) แยกจาก Overview เดิม
- Revenue chart ใน Overview เพิ่ม **filter tabs: Week / Month / Custom** พร้อม date range picker

### Race Operations Layout (3 rows)
| Row | Card ซ้าย | Card ขวา |
|---|---|---|
| 1 | Registration Fill Rate (4 races) | Runner Demographics |
| 2 | Top Nationalities | Shirt Size Summary |
| 3 | Recent Activity Feed (full width) | — |

### รายละเอียดแต่ละ Card
**Registration Fill Rate**
- ขยายจาก 2 → 4 races: 100K Ultra, 50K Trail, 25K Fun Run, 10K Sprint
- bar เปลี่ยนสีเป็น warning (orange) เมื่อ fill rate >= 80%

**Runner Demographics**
- ลบ Thai/International breakdown เดิมออก
- เพิ่ม Age Range bars (Under 30 / 30–39 / 40–49 / 50+)
- เพิ่ม Gender dual-color bar (Blue = Male, Pink = Female)

**Top Nationalities**
- เปลี่ยนจาก pie chart → flag emoji + rank list + horizontal bar
- แสดง Top 5 เรียงจากมากไปน้อย

**Recent Activity Feed**
- เพิ่มใหม่: แสดง 5 รายการล่าสุด (registration / payment / cancellation / refund)

### Mock Data ที่เพิ่ม (`src/data/mockData.ts`)
- เพิ่ม `age: number` field ใน `Participant` interface
- อัปเดต mockParticipants ทั้ง 8 คนให้มี age (28–52)
- อัปเดต `mockCategoryFillRate` เป็น 4 races
- เพิ่ม `revenueWeeklyData` (Mon–Sun)
- เพิ่ม `revenueMonthlyData` (Week 1–4)
- เพิ่ม `mockRecentActivity` (5 items)

### Cards ที่ลบออก
- Unpaid Registrations
- Race Day Readiness

---

## 2. EventManagerHub — Orders (Finance)

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### สิ่งที่เปลี่ยน
**Type System (`mockData.ts`)**
- ลบ `status: 'completed' | 'pending' | 'refunded'` (3 ค่าเดิม)
- เพิ่ม `export type OrderStatus` ครอบคลุม 22 status จากระบบบัญชีจริง

**Status Groups**
| กลุ่ม | สี | ตัวอย่าง |
|---|---|---|
| เสร็จสมบูรณ์ | green | complete_trc_issued, complete_receipt_issued |
| รอดำเนินการ | blue | submitted, complete_wait_trc, complete_stripe_wait_receipt |
| รอตรวจสอบ | yellow | pending_cash, pending_refund, edit_trc |
| มีปัญหา / คืนเงิน | red | issue_cash, issue_refund, refunded |
| พิเศษ | purple | complete_vip, complete_sponsor |
| หมดสิทธิ์วิ่ง | gray | complete_wns, complete_wns_receipt |

**Mock Data (`mockData.ts`)**
- ขยาย mockOrders จาก 7 → 14 records ครอบคลุมหลาย status

**UI (`EventManagerHub.tsx`)**
- เพิ่ม `ORDER_STATUS_LABEL` map — แสดง Thai label แทน raw key
- เพิ่ม `ORDER_STATUS_COLOR` map — Tailwind classes ตามกลุ่ม
- เพิ่ม filter tabs: **All | Completed | Pending | Issues | Refunds | Special**
- Status badge แสดง Thai label พร้อมสีถูกต้อง
- Action "Refund" แสดงเฉพาะ status ที่ complete จริง (`complete_trc_issued`, `complete_receipt_issued`)
- เพิ่ม `overflow-x-auto` ให้ตารางรองรับ label ยาว

---

## 3. EventManagerHub — Participants: Search, Sort, Edit Modal

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### Mock Data
- เพิ่ม fields ใน `Participant` interface: `phone`, `bloodType`, `medicalConditions`, `club`, `itraId`
- อัปเดต mockParticipants ทั้ง 8 คนให้มี fields ใหม่ครบ

### UI
- เพิ่ม search bar ค้นหาชื่อหรืออีเมล
- Column headers clickable สำหรับ sort (BIB, Name, Distance, Gender, Shirt, Nationality) พร้อม icon asc/desc
- ลบปุ่ม "Assign BIB" ออก เปลี่ยนเป็น icon Pencil
- เพิ่ม Edit Modal (Dialog) แก้ข้อมูลทั้งหมด: BIB, Distance, Name, Email, Phone, Age, Gender, Nationality, Shirt Size, Blood Type, Emergency Contact, Club/Team, ITRA ID, Medical Conditions
- `participants` ใช้ local state เพื่อ save ผลจาก edit modal ได้

---

## 4. Participants: 100 คน + Advanced Filters + Race Operations Revenue

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### Mock Data
- ขยาย mockParticipants จาก 8 → 100 คน (100K: 34, 50K: 45, 25K: 21)
- ครอบคลุม 17 สัญชาติ: TH, JP, US, SG, DE, AU, KR, FR, GB, HK, CN, MY, PH, NZ, CA, ZA
- ทุกคนมีข้อมูลครบ: phone, bloodType, medicalConditions, emergencyContact, club, itraId

### Participants Filters
- เพิ่ม state: `genderFilter`, `ageFilter`, `nationalityFilter`
- เปลี่ยน UI จาก Tabs เป็น Select dropdowns 4 ตัว: Distance / Gender / Age / Nationality
- Nationality dropdown สร้างจาก unique values ใน data จริง (dynamic)
- เพิ่ม participant count badge "X / 100"
- เพิ่มปุ่ม "Clear" เมื่อมี filter ใด active

### Race Operations Revenue
- เพิ่ม 3 stat cards (Total Revenue, Tickets Sold, Capacity) ด้านบน
- เพิ่ม Revenue line chart พร้อม Week/Month/Custom filter (share state กับ Overview)

---

## 6. EventManagerHub — Orders (Finance): 3-dot Menu, Note Column, Editable Status

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### สิ่งที่เปลี่ยน

**Mock Data (`mockData.ts`)**
- เพิ่ม `note: string` ใน `Order` interface
- อัปเดต mockOrders ทั้ง 14 records ให้มี `note` (บาง record มี note ตัวอย่างเช่น "Cancelled due to injury", "DNS - notified organizer")

**UI (`EventManagerHub.tsx`)**
- เพิ่ม `orders` local state (useState จาก mockOrders) เพื่อรองรับการแก้ไข note และ status
- Import `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuTrigger` จาก shadcn/ui
- Import `MoreHorizontal` จาก lucide-react
- **Action column**: เปลี่ยนจาก conditional Refund button เป็น 3-dot icon (`MoreHorizontal`) DropdownMenu พร้อมรายการ:
  1. Refund
  2. เปลี่ยนแปลงระยะวิ่ง
  3. Issue Tax Invoice
- **Note column**: เพิ่ม column ใหม่ — inline input ที่แก้ไขได้เลย (hover แสดง border, focus แสดง ring)
- **Status column**: เปลี่ยนจาก badge ธรรมดา เป็น Select dropdown — admin กดเลือก status ใดๆ ได้จาก ALL_STATUSES รายการครบทั้ง 22 statuses พร้อมสีตรงกัน
- เพิ่ม `updateOrderStatus()` และ `updateOrderNote()` helper functions
- เพิ่ม `ALL_STATUSES` constant (Object.keys ของ ORDER_STATUS_LABEL)
- colSpan เปลี่ยนจาก 8 → 9 (เพิ่ม Note column)

---

## 5. EventManagerHub — Orders (Finance) เพิ่มเติม

**ไฟล์ที่แก้:** `src/views/EventManagerHub.tsx`, `src/data/mockData.ts`

### สิ่งที่เพิ่ม

**Summary Stats (4 mini-cards เหนือตาราง)**
- **Total Collected** — รวมยอดเงินที่ได้รับ (สีเขียว)
- **Pending Review** — จำนวน order ที่รอดำเนินการ (สีเหลือง)
- **Issues** — จำนวน order ที่มีปัญหา (สีแดง)
- **Refunded** — รวมยอดเงินที่คืน (สีเทา)

**Search Bar**
- ค้นหาได้จาก ชื่อ / email / Order ID
- วางข้าง filter tabs (responsive)

**Payment Method column**
- เพิ่ม `paymentMethod: 'Stripe' | 'Cash' | 'VIP' | 'Sponsor'` ใน Order interface
- badge สี: Stripe=blue, Cash=amber, VIP=purple, Sponsor=emerald
- อัปเดต mockOrders ทั้ง 14 records ให้มี paymentMethod
- เพิ่มเป็น column ในตารางระหว่าง Category และ Amount
